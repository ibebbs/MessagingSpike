<Window x:Class="MessagingSpike.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Location:" Margin="4"/>
            <TextBlock Text="{Binding Path=Location.Y, FallbackValue='Latitude: 0.00', StringFormat='Latitude: {0}'}" Margin="4"/>
            <TextBlock Text="{Binding Path=Location.X, FallbackValue='Longitude: 0.00', StringFormat='Longitude: {0}'}" Margin="4"/>
        </StackPanel>
    </Grid>

</Window>